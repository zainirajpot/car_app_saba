import 'package:car_app_saba/comman/app_colors.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AppIcons {
  static const shopping = Icon(
    Icons.shopping_bag_outlined,
    color: AppColors.otherText,
  );
  static const camera = Icon(
    Icons.camera_alt_outlined,
    size: 38,
    color: AppColors.otherText,
  );

  static const eye = Icon(
    Icons.remove_red_eye,
    color: AppColors.otherText,
  );
  static const home = Icon(
    Icons.home,
    size: 20,
    color: AppColors.otherText,
  );
  static const message = Icon(
    Icons.message_outlined,
    color: AppColors.otherText,
    size: 20,
  );
  static const music = Icon(
    CupertinoIcons.music_note_2,
    color: AppColors.otherText,
    size: 20,
  );
  static const notification = Icon(
    Icons.notifications_none,
    color: AppColors.otherText,
    size: 20,
  );
  static const line = Icon(
    Icons.line_axis_rounded,
    color: AppColors.otherText,
    size: 20,
  );
}
